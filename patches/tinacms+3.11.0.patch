diff --git a/node_modules/tinacms/dist/index.js b/node_modules/tinacms/dist/index.js
index 69093c7..e4981f1 100644
--- a/node_modules/tinacms/dist/index.js
+++ b/node_modules/tinacms/dist/index.js
@@ -6187,12 +6187,7 @@ const Group$1 = ({ tinaForm, form, field, input, meta, index }) => {
       form.mutators.push(field.name, obj);
     }
     if (field.openFormOnCreate) {
-      const state = tinaForm.finalForm.getState();
       const newIndex = field.addItemBehavior === "prepend" ? 0 : items.length;
-      if (state.invalid === true) {
-        cms.alerts.error("Cannot navigate away from an invalid form.");
-        return;
-      }
       cms.dispatch({
         type: "forms:set-active-field-name",
         value: {
@@ -6300,13 +6295,6 @@ const Item$1 = ({
         }),
         onMouseOut: () => setHoveredField({ id: null, fieldName: null }),
         onClick: () => {
-          const state = tinaForm.finalForm.getState();
-          if (state.invalid === true) {
-            cms.alerts.error(
-              "Cannot navigate away from an invalid form."
-            );
-            return;
-          }
           cms.dispatch({
             type: "forms:set-active-field-name",
             value: {
@@ -6467,11 +6455,6 @@ const Group = wrapFieldWithNoHeader(
       Header,
       {
         onClick: () => {
-          const state = tinaForm.finalForm.getState();
-          if (state.invalid === true) {
-            cms.alerts.error("Cannot navigate away from an invalid form.");
-            return;
-          }
           cms.dispatch({
             type: "forms:set-active-field-name",
             value: { formId: tinaForm.id, fieldName: field.name }
@@ -6915,13 +6898,6 @@ const BlockListItem = ({
     ItemClickTarget,
     {
       onClick: () => {
-        const state = tinaForm.finalForm.getState();
-        if (state.invalid === true) {
-          cms.alerts.error(
-            "Cannot navigate away from an invalid form."
-          );
-          return;
-        }
         cms.dispatch({
           type: "forms:set-active-field-name",
           value: {
@@ -17143,6 +17119,21 @@ const FormBuilder = ({
     };
   }, [finalForm]);
   const fieldGroup = tinaForm.getActiveField(form.activeFieldName);
+  const nestedParentFieldName = form.activeFieldName ? cms.state.breadcrumbs.at(-2)?.formName ?? "" : null;
+  const completeNestedItem = () => {
+    if (nestedParentFieldName === null)
+      return;
+    cms.dispatch({
+      type: "forms:set-active-field-name",
+      value: {
+        formId: tinaForm.id,
+        fieldName: nestedParentFieldName
+      }
+    });
+    cms.alerts.info(
+      'Item concluído. Clique em "Salvar documento" para publicar as alterações.'
+    );
+  };
   return /* @__PURE__ */ React.createElement(
     Form$1,
     {
@@ -17279,7 +17270,18 @@ const FormBuilder = ({
           activeFieldName: form.activeFieldName,
           fields: fieldGroup.fields
         }
-      ) : /* @__PURE__ */ React.createElement(NoFieldsPlaceholder, null))), !hideFooter && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(RelatedFilesBanner, null), /* @__PURE__ */ React.createElement("div", { className: "relative flex-none w-full h-16 px-6 bg-white border-t border-gray-100 flex items-center justify-end" }, /* @__PURE__ */ React.createElement("div", { className: "flex-1 w-full justify-end gap-2	flex items-center max-w-form" }, tinaForm.reset && /* @__PURE__ */ React.createElement(
+      ) : /* @__PURE__ */ React.createElement(NoFieldsPlaceholder, null))), !hideFooter && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(RelatedFilesBanner, null), /* @__PURE__ */ React.createElement("div", { className: "relative flex-none w-full h-16 px-6 bg-white border-t border-gray-100 flex items-center justify-end" }, /* @__PURE__ */ React.createElement("div", { className: "flex-1 w-full justify-end gap-2	flex items-center max-w-form" }, nestedParentFieldName !== null && /* @__PURE__ */ React.createElement(
+        Button$2,
+        {
+          type: "button",
+          variant: "secondary",
+          className: "gap-2",
+          title: "As alterações deste item ficam no formulário. Use Salvar documento para gravá-las.",
+          onClick: completeNestedItem
+        },
+        /* @__PURE__ */ React.createElement(BiArrowBack, { className: "w-4 h-4" }),
+        "Concluir item"
+      ), tinaForm.reset && /* @__PURE__ */ React.createElement(
         ResetForm,
         {
           pristine,
@@ -17298,7 +17300,7 @@ const FormBuilder = ({
           busy: submitting,
           variant: "primary"
         },
-        tinaForm.buttons.save
+        nestedParentFieldName !== null ? "Salvar documento" : tinaForm.buttons.save
       ), tinaForm.actions.length > 0 && /* @__PURE__ */ React.createElement(
         FormActionMenu,
         {
